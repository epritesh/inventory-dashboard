Write-Host 'This project now deploys the web client via Slate (GitHub) only.' -ForegroundColor Yellow
Write-Host 'Use the Slate pipeline (auto-deploy on main) to publish changes.' -ForegroundColor Yellow
Write-Host 'Local preview: `npm --prefix web run dev`' -ForegroundColor Yellow
